#-quicklisp
(let ((quicklisp-init #P"/usr/local/src/multipart-form-data/.quicklisp/setup.lisp"))
  (when (probe-file quicklisp-init)
      (load quicklisp-init)))

#+quicklisp
(push #P"/usr/local/src/multipart-form-data" ql:*local-project-directories*)
